# 模型请求数据追溯 — 设计与实现

> 日期：2026-08-28
> 范围：`jarvis/services/ai/call_logger.py` + `jarvis/api/logs.py` + `frontend/src/components/LLMCallLogViewer.vue` + 周边接入
> 状态：✅ 已落地，前后端 type/test 验证通过

---

## 1. 目标

为每一次 LLM 调用提供**完整的请求/响应追溯能力**：

| 维度 | 期望 |
|------|------|
| **请求可追溯** | 记录每次调用的 messages 列表 + 工具 schema + provider/model |
| **响应可追溯** | 记录 content / thinking / content_blocks (含 tool_use) / usage / stop_reason |
| **原始 HTTP body** | 记录实际发给 provider 的 payload（model + max_tokens + tools 摘要）和完整原始 response |
| **SSE 流式 chunk** | 流式调用时记录每一条 SSE 事件，便于复盘工具调用参数拼装过程 |
| **按会话聚合** | 一键查看"这个对话跑了多少次 LLM 调用"，每条的 latency/状态 |
| **失败可定位** | 区分 success / error / stream_interrupted，记录异常 message |

---

## 2. 架构总览

```
                  ┌──────────────────────────────────────────────────────────┐
                  │                  jarvis/api/logs.py                       │
   GET /logs/...  │   ├─ GET  /logs/llm_calls/dates                            │
   ────────────►  │   ├─ GET  /logs/llm_calls?date=&conversation_id=...       │
                  │   ├─ GET  /logs/llm_calls/{call_id}                        │
                  │   ├─ DEL  /logs/llm_calls?date=                           │
                  │   └─ GET  /logs/llm_calls_stats/summary                    │
                  └────────────────────┬─────────────────────────────────────┘
                                       ▼
                  ┌──────────────────────────────────────────────────────────┐
                  │        jarvis/services/ai/call_logger.py                  │
                  │   LLMCallLogger (单例)                                     │
                  │     ├─ start_call() → LLMCallRecord                       │
                  │     ├─ end_call()   → async flush (asyncio.create_task)   │
                  │     ├─ trace()     → 上下文管理器                         │
                  │     ├─ list_calls() / get_call() / clear()                │
                  │     └─ _records: dict (in-memory, 供 adapter 反查)        │
                  │                                                          │
                  │   contextvars.ContextVar: _current_call_id               │
                  │   helper: get/set/reset_current_call_id, enrich_raw_body │
                  └────────────────────┬─────────────────────────────────────┘
                                       ▲
                                       │ (start_call / end_call)
                                       │
       ┌───────────────────────────────┼───────────────────────────────────┐
       │            ┌──────────────────┴──────────────────┐                │
       │            │       jarvis/services/ai/router.py  │                │
       │            │   AIRouter.chat / chat_stream_full  │                │
       │            │     ├─ cl.start_call(...)            │                │
       │            │     ├─ set_current_call_id(token)   │                │
       │            │     ├─ client.chat(...) / stream     │                │
       │            │     ├─ cl.end_call(...)              │                │
       │            │     └─ reset_current_call_id(token)  │                │
       │            └──────────────────┬──────────────────┘                │
       │                               │                                    │
       │                               ▼ (adapter 通过 contextvar 拿到 id)  │
       │            ┌──────────────────────────────────────────────────┐   │
       │            │       jarvis/services/ai/providers/               │   │
       │            │   AnthropicAdapter / OpenAIAdapter                │   │
       │            │     ├─ cid = get_current_call_id()                │   │
       │            │     ├─ enrich_raw_body(cid, raw_request=payload)   │   │
       │            │     ├─ [非流] enrich_raw_body(cid, raw_response)   │   │
       │            │     └─ [流]   累积 raw_sse_chunks, 末尾 enrich     │   │
       │            └──────────────────────────────────────────────────┘   │
       │                                                                   │
       └───────────────────────────────────────────────────────────────────┘

                                       ▼

                  ┌──────────────────────────────────────────────────────────┐
                  │              文件存储: logs/llm_calls/                    │
                  │   YYYY-MM-DD/                                            │
                  │     ├── index.jsonl         # 一行一个调用摘要 (列表用)   │
                  │     └── cl-xxxxxxxxxx.json  # 单次调用完整记录            │
                  └──────────────────────────────────────────────────────────┘
```

---

## 3. 三层捕获粒度

设计核心：**同一个 `call_id` 在三层都被打点**，通过 `contextvars.ContextVar` 关联，无需改 adapter 函数签名。

| 层级 | 捕获字段 | 文件位置 |
|------|----------|----------|
| **业务层 (Router)** | `messages` 列表 / `content_blocks` (含 tool_use) / `thinking` / `usage` / `latency_ms` / `conversation_id` / `provider_protocol` | `jarvis/services/ai/router.py` |
| **协议层 (Adapter)** | raw HTTP `request payload` 摘要 (model + max_tokens + tools_count) / raw `response body` / 流式场景的所有 SSE chunks | `jarvis/services/ai/providers/anthropic.py` & `openai.py` |
| **持久化层 (CallLogger)** | 拼装 record → 异步 flush → 写 `index.jsonl` + `<call_id>.json` | `jarvis/services/ai/call_logger.py` |

### 3.1 为什么需要三层？

- **业务层**让你看到"LLM 视角"的输入输出 — Anthropic 与 OpenAI 协议不同，但 messages 抽象统一
- **协议层**让你看到"provider 视角"的真实 HTTP 交互 — 排查 4xx/5xx、流式 SSE 解析错误时只能看这个
- **持久化层**让你能按日期/会话/状态查询 — 索引与详情分离，列表只读 index，点开再读详情

### 3.2 ContextVar 透传原理

```python
# call_logger.py
_current_call_id: contextvars.ContextVar[Optional[str]] = contextvars.ContextVar(
    "jarvis_current_llm_call_id", default=None
)

# Router:
ctx_token = set_current_call_id(rec.call_id)
try:
    resp = await client.chat(messages, ...)
    cl.end_call(rec, response=...)
finally:
    reset_current_call_id(ctx_token)

# Adapter (签名零修改):
cid = get_current_call_id()
if cid is not None:
    enrich_raw_body(cid, raw_request=payload)
    ...
```

asyncio 单线程模型下，contextvar 跟随 task 隔离，**并发调用不会串**。

---

## 4. 存储模型

### 4.1 目录布局

```
logs/llm_calls/
└── 2026-08-28/
    ├── index.jsonl                      # 一行一个调用摘要
    └── cl-8f255c9c6e.json               # 完整记录
```

按日期分目录便于：
- 单日清空 (`DELETE /logs/llm_calls?date=2026-08-28`)
- 按日期过滤 (`GET /logs/llm_calls?date=2026-08-28`)
- 避免单目录文件过多（FS inode 压力）

### 4.2 Call ID 格式

```
cl-<sha1[:10]>
```

例: `cl-8f255c9c6e`

- 全局唯一（基于 `timestamp_ms + uuid`）
- 时间序可读（hash 与 timestamp 间接相关）
- 短（10 hex = 40 bit，碰撞概率忽略）

### 4.3 index.jsonl 单行格式

```json
{
  "call_id": "cl-8f255c9c6e",
  "timestamp": "2026-08-28T22:20:20.498",
  "timestamp_ms": 1787926820498,
  "model": "qwen3:4b",
  "provider": "ollama",
  "provider_protocol": "anthropic",
  "conversation_id": "conv_abc123",
  "source": "router.chat_stream_full",
  "latency_ms": 920.4,
  "status": "success",
  "messages_count": 4,
  "has_tool_use": true,
  "thinking_chars": 280,
  "response_chars": 150,
  "error": null
}
```

**只含摘要**，不含 messages / content_blocks — 列表 API 不读详情文件。

### 4.4 `<call_id>.json` 完整记录

```json
{
  "call_id": "cl-8f255c9c6e",
  "timestamp": "2026-08-28T22:20:20.498",
  "timestamp_ms": 1787926820498,
  "model": "qwen3:4b",
  "provider": "ollama",
  "provider_protocol": "anthropic",
  "conversation_id": "conv_abc123",
  "source": "router.chat_stream_full",
  "latency_ms": 920.4,
  "status": "success",
  "error": null,
  "request": {
    "messages": [
      {"role": "system", "content": "你是贾维斯..."},
      {"role": "user", "content": "读一下 /etc/hosts"}
    ],
    "stream": true,
    "max_tokens": 4096,
    "temperature": null,
    "raw_http_body": {
      "model": "qwen3:4b",
      "max_tokens": 4096,
      "stream": true,
      "messages_count": 2,
      "tools_count": 7
    }
  },
  "response": {
    "content": "文件内容是 ...",
    "thinking": "用户想看 hosts，我先调 file.read...",
    "content_blocks": [
      {"type": "thinking", "thinking": "用户想看 hosts..."},
      {"type": "tool_use", "id": "toolu_xxx", "name": "file", "input": {"action": "read", "path": "/etc/hosts"}},
      {"type": "text", "text": "我先调 file.read"}
    ],
    "usage": {"prompt_tokens": 234, "completion_tokens": 56, "total_tokens": 290},
    "raw_stream_events": [
      {"type": "message_start", ...},
      {"type": "content_block_start", ...},
      ...
      {"type": "message_stop", ...}
    ],
    "stop_reason": "tool_use"
  },
  "metadata": {"provider_hint": null}
}
```

### 4.5 流式 vs 非流的差异

| 字段 | 非流 (`router.chat()`) | 流式 (`router.chat_stream_full()`) |
|------|----------------------|-----------------------------------|
| `request.raw_http_body` | ✅ | ✅ |
| `response.raw_http_body` | ✅ 完整 response JSON | ❌（流式没有单次完整响应）|
| `response.raw_stream_events` | ❌ | ✅ 所有 SSE chunks |
| `response.content_blocks` | ✅（router 从 `resp.content_blocks` 拿）| ✅（router 从事件流重建）|
| `response.thinking` | ✅（如 provider 返回）| ✅（从 `thinking` 事件累积）|

---

## 5. 后端 API

### 5.1 端点清单

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/logs/llm_calls/dates` | 列出所有有日志的日期 |
| GET | `/api/logs/llm_calls` | 列表（按日期 + 分页 + 过滤）|
| GET | `/api/logs/llm_calls/{call_id}` | 单条完整详情 |
| DELETE | `/api/logs/llm_calls` | 清空（可指定日期）|
| GET | `/api/logs/llm_calls_stats/summary` | 统计摘要（错误率 / p50/p95 latency）|

### 5.2 GET `/api/logs/llm_calls` 过滤参数

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `date` | `YYYY-MM-DD` | 今天 | 必传或默认今天 |
| `limit` | 1-1000 | 100 | 分页大小 |
| `offset` | ≥0 | 0 | 分页偏移 |
| `conversation_id` | string | - | 按会话过滤 |
| `provider` | string | - | 按 provider (ollama/openai/anthropic) |
| `model` | string | - | 按模型名 |
| `status` | string | - | success / error / stream_interrupted |

### 5.3 响应示例

```json
{
  "date": "2026-08-28",
  "total": 142,
  "offset": 0,
  "limit": 50,
  "items": [
    {
      "call_id": "cl-8f255c9c6e",
      "timestamp": "2026-08-28T22:20:20.498",
      "model": "qwen3:4b",
      "provider": "ollama",
      "provider_protocol": "anthropic",
      "conversation_id": "conv_abc123",
      "source": "router.chat_stream_full",
      "latency_ms": 920.4,
      "status": "success",
      "messages_count": 4,
      "has_tool_use": true,
      "thinking_chars": 280,
      "response_chars": 150,
      "error": null
    },
    ...
  ]
}
```

### 5.4 统计响应

```json
{
  "date": "2026-08-28",
  "total": 142,
  "by_status": {"success": 138, "error": 4},
  "by_provider": {"ollama": 140, "openai": 2},
  "by_model": {"qwen3:4b": 140, "gpt-4o-mini": 2},
  "latency_ms": {"avg": 850.2, "p50": 720, "p95": 1850, "count": 142},
  "error_rate": 0.028
}
```

---

## 6. 前端 UI

### 6.1 入口

`Settings.vue` 侧栏新增 "LLM 日志" section，嵌入 `LLMCallLogViewer.vue`。

### 6.2 LLMCallLogViewer 布局

```
┌──────────────────────────────────────────────────────┬──────────────────────────────────────────────────────┐
│ 日期 [2026-08-28 ▾]                    [清空]         │                                                  ✕  │
│ 共 142 次 | 错误率 2.8% | avg 850ms | p95 1850ms     │  cl-8f255c9c6e                          [success]    │
│                              [按时间|按会话 (8)] [展开]│  开始 2026-08-28 22:20:20.498                       │
│ ┌─ 过滤 ───────────────────────────────────────────┐ │  耗时 920 ms                                       │
│ │ [provider][model][conversation_id][status ▾]    │ │  provider ollama (anthropic)                        │
│ └──────────────────────────────────────────────────┘ │  model qwen3:4b                                    │
│                                                      │  conv conv_abc123                                   │
│ ──── 按时间模式 ─────────────────────────────        │                                                      │
│ #  | 时间    | provider | model   | ms   | 状态      │  [Request Body] [Response] [Raw HTTP] [Messages]    │
│ 1  | 22:20.498| ollama  | qwen3   | 920  | ✓ 🔧     │                                                      │
│ 2  | 22:19.331| ollama  | qwen3   | 880  | ✓        │  {                                                   │
│ 3  | 22:18.012| ollama  | qwen3   | 950  | ✗        │    "model": "qwen3:4b",                              │
│ ...                                                  │    "provider": "ollama",                             │
│                                                      │    "provider_protocol": "anthropic",                │
│ ──── 按会话模式 ─────────────────────────────        │    "stream": true,                                   │
│ ▶ conv_a1b2...c3d4  [3 次] [有错误]                 │    "max_tokens": 4096,                              │
│   qwen3:4b  22:10 → 22:15  Σ 2750ms    [过滤]       │    "messages_count": 2,                              │
│   ┌──────────────────────────────────────────────┐ │    "tools_count": 7                                  │
│   │ 22:15  ollama qwen3  920ms ✓                 │ │  }                                                   │
│   │ 22:13  ollama qwen3  880ms ✓ 🔧              │ │                                                      │
│   │ 22:10  ollama qwen3  950ms ✗                 │ │  (Request Body Tab — 摘要级)                        │
│   └──────────────────────────────────────────────┘ │                                                      │
│ ▶ conv_e5f6...g7h8  [5 次]                          │                                                      │
│   qwen3:4b, claude-3-5-sonnet  22:00 → 22:08        │                                                      │
│ ▶ (无会话 ID) [2 次]                                │                                                      │
│                                                      │                                                      │
│ 1-50 / 142                              [←] [→]      │                                                      │
└──────────────────────────────────────────────────────┴──────────────────────────────────────────────────────┘
```

### 6.3 详情 Tabs

| Tab | 内容 | 何时用 |
|-----|------|--------|
| **Request Body** | `model` / `provider_protocol` / `stream` / `max_tokens` / `messages_count` / `tools_count` 摘要 | 快速看"这次调用带了什么" |
| **Response** | `content` / `thinking` / `stop_reason` / `content_blocks` / `usage` | 排查 LLM 思考过程 + 工具调用决策 |
| **Raw HTTP** | `request_body` (model + max_tokens + tools 摘要) / `response_body` 或 `stream_events` (前 5 个) | 排查 4xx/5xx / 协议层解析错误 |
| **Messages** | 完整 `messages` 列表，每条按 role 彩色标签分组渲染 | 排查上下文污染 / 漏写 assistant turn |

### 6.4 视图模式

| 模式 | 行为 | 何时用 |
|------|------|--------|
| **按时间** (默认) | 平铺表格，按 call 时间倒序 | 监控 / 时序分析 |
| **按会话** | 按 `conversation_id` 折叠分组，显示每组聚合 | 复盘一次对话的工具循环 / 错误定位 |

按会话模式额外能力：
- 组头显示：会话 ID 短 hash + 调用次数 + 时间范围 + 总耗时 + 是否有错误 + 用过的模型
- 顶部 "展开/折叠" 一键操作所有 group
- 每个 group 头部 "过滤" 按钮 → 自动跳到按时间模式并预填该会话 ID

---

## 7. 关键设计点

### 7.1 异步 flush 不阻塞主流程

```python
def end_call(self, record, *, response=None, status="success", error=None):
    record.latency_ms = (time.time() * 1000) - record.timestamp_ms
    record.status = status
    record.error = error
    if response:
        for k, v in response.items():
            record.response[k] = v
    with self._record_lock:
        self._records.pop(record.call_id, None)
    # fire-and-forget — 不阻塞 chat 主流程
    asyncio.create_task(self._flush(record))

async def _flush(self, record):
    try:
        await asyncio.to_thread(self._write_sync, record)
    except Exception as e:
        logger.error(f"[CallLogger] flush failed for {record.call_id}: {e}")
```

- `asyncio.create_task` 不阻塞 `end_call()` 调用方
- `asyncio.to_thread` 把同步写文件放到线程池，不阻塞 event loop
- 写入异常只 log 警告，不影响业务

### 7.2 In-memory `_records` + ContextVar

```python
self._records: dict[str, LLMCallRecord] = {}  # start_call → end_call 之间存在
self._record_lock = threading.Lock()
```

- Adapter 通过 `contextvar` 反查到 record 调 `enrich_raw_body`，无需传递 record 引用
- `end_call` 时从 dict 移除（自动 GC）
- 异常路径走 `finally` 也能清理

### 7.3 路径穿越防护

```python
def get_call(self, call_id: str) -> Optional[dict]:
    if not call_id or "/" in call_id or ".." in call_id:
        return None
    ...
```

防止恶意 call_id 读取日志目录外的文件。

### 7.4 索引与详情分离

- 列表 API 只读 `index.jsonl`（轻量、几十字节/行）
- 详情 API 才读 `<call_id>.json`（可能几十 KB）
- 单日 1000 次调用 = index ~50KB，详情 ~50MB
- 列表页翻页不会因详情文件大而卡顿

### 7.5 写入并发控制

```python
self._write_lock = threading.Lock()  # JSONL append, 跨线程安全
```

- 同一文件的多次 flush 通过锁串行化
- 不同 call_id 不同文件，并发安全

### 7.6 异常路径也保留信息

```python
except httpx.HTTPError as e:
    if cid is not None and raw_sse_chunks:
        enrich_raw_body(cid, raw_stream_events=raw_sse_chunks)
    raise ProviderNotAvailableError(...)
```

流式场景中途失败，已收到的 SSE chunks 仍写盘 — 排查"为何断流"。

---

## 8. 与现有架构的契合点

### 8.1 与 AgentLoopRunner 的关系

`AgentLoopRunner.run_iterations()` 的每一次 `router.chat(stream=False)` 都会触发 `call_logger.start_call/end_call`。

```
[ChatEngine.stream_chat]
    ↓ (Phase 1) router.chat_stream_full → 1 次调用 (1 条日志)
    ↓ (Phase 2+) runner.run_iterations
                ↓ iter 2: router.chat(stream=False) → 1 条日志
                ↓ iter 3: router.chat(stream=False) → 1 条日志
                ↓ (假设调 3 次工具) 总共 4 条日志, conversation_id 全相同
```

### 8.2 与 SubagentOrchestrator 的关系

Subagent 调 `router.chat` 时**没有**显式传 `conversation_id`（目前 subagent 是独立会话），所以记录会落到 `(no-conv)` 分组。

**已知限制**：subagent 调用无法直接关联到父会话。如果需要追溯"主对话 → subagent → 它的工具循环"，需要给 subagent 显式传父 conversation_id，作为后续增强。

### 8.3 与前端 ChatWindow 的关系

ChatWindow 当前不感知 call_id。**前端无法点击"查看本次对话的所有 LLM 调用"直接跳到 viewer**——这是已知 UX gap，可作为后续增强：

```typescript
// 设想: ChatWindow 顶部加按钮
function openCallLogs() {
  chatStore.setFilter({ conversation_id: currentConvId })
  router.push('/settings?section=llm_logs')
}
```

---

## 9. 测试覆盖

### 9.1 后端集成验证

```bash
$ python -c "from jarvis.services.ai.call_logger import get_call_logger; ..."
✓ call_logger imports OK
✓ call_logger write OK
✓ list_calls OK: 1 entries
✓ get_call OK: call_id=cl-8f255c9c6e
✓ logs router: 5 endpoints
✓ api_router: 11 endpoints total
```

### 9.2 现有测试无 regression

```bash
$ python -m pytest tests/test_chat_engine.py -x
35 passed, 2 warnings in 21.49s
```

### 9.3 前端类型检查

```bash
$ npx vue-tsc --noEmit -p tsconfig.json
(0 errors)
```

### 9.4 单元测试缺口

- ❌ 没有针对 `call_logger` 的单元测试（仅集成验证）
- ❌ 没有针对 `logs.py` API 的测试
- 后续可补 `tests/test_call_logger.py` + `tests/test_logs_api.py`

---

## 10. 文件清单

### 新增（4 个）

| 文件 | 行数 | 内容 |
|------|------|------|
| `jarvis/services/ai/call_logger.py` | ~390 | LLMCallLogger + LLMCallRecord + ContextVar + helper |
| `jarvis/api/logs.py` | ~95 | 5 个 API 端点 |
| `frontend/src/stores/llmCallLogs.ts` | ~210 | Pinia store + groupedByConversation computed |
| `frontend/src/components/LLMCallLogViewer.vue` | ~430 | 时间/会话双视图 + 详情 4 tab |

### 修改（9 个）

| 文件 | 关键改动 |
|------|---------|
| `jarvis/services/ai/router.py` | `chat()` / `chat_stream_full()` 加 call_logger 拦截 + ContextVar 透传 |
| `jarvis/services/ai/providers/anthropic.py` | 捕获 raw HTTP request + response + 流式 SSE chunks |
| `jarvis/services/ai/providers/openai.py` | 同上 |
| `jarvis/core/chat_engine.py` | 三处入口（chat / stream_chat / stream_chat_with_messages）传 conversation_id |
| `jarvis/core/agent_loop.py` | run_iterations 透传 conversation_id |
| `jarvis/api/routes.py` | 注册 logs_router |
| `frontend/src/composables/useApi.ts` | 5 个新 API 方法 |
| `frontend/src/types/index.ts` | 4 个新 TypeScript 类型 |
| `frontend/src/components/Settings.vue` | 侧栏加 "LLM 日志" section + 嵌入 viewer |

---

## 11. 典型使用场景

### 11.1 排查"为什么这个对话跑了 30 秒才返回"

1. 进 Settings → LLM 日志
2. 默认按时间模式，找到耗时 > 5000ms 的行
3. 看 conversation_id — 确认是不是当前对话
4. 点开看详情
5. **Raw HTTP** tab → 看 `request_body` 是否带了过多 tools（影响 tool_choice 决策时间）
6. **Response** tab → 看 `content_blocks` 有几个 tool_use（多 = 多轮迭代）
7. **Messages** tab → 看 messages_count 是否过大（= 上下文管理有问题）

### 11.2 复盘"subagent 调研的结果"

1. 按会话模式
2. 找父对话（conv_id 已知）
3. 展开看到 8 次调用
4. 看每次调用的 model — 是否切换到了正确的 subagent model
5. 看 status — 是否中途有 error

### 11.3 监控错误率

1. 顶部看"错误率 X%" + avg/p95 latency
2. 切到按 status=error 过滤
3. 看具体哪些调用失败 — 是否同一 provider / 同一 model / 同一时间集中

### 11.4 清理老日志

1. 选日期 → [清空]
2. 确认 → 删除该日期所有详情 + index
3. 列表自动刷新为空

---

## 12. 后续增强

| 优先级 | 增强 | 说明 |
|--------|------|------|
| P1 | subagent 调 LLM 时传父 conversation_id | 让 subagent 工具循环的日志也归到主对话 |
| P1 | ChatWindow 加"查看本次对话的 LLM 调用"按钮 | 一键跳 viewer 并预填 conversation_id |
| P2 | 增加 `tests/test_call_logger.py` + `tests/test_logs_api.py` | 单元测试覆盖 |
| P2 | 日志按会话索引（`by_conversation/<conv_id>.jsonl`）| 大数据量下会话查询更快 |
| P3 | 日志自动轮转 / 压缩 | 避免长期运行后日志目录过大 |
| P3 | 详情 JSON 美化高亮（syntax highlight） | 提升可读性 |
| P3 | 导出 / 分享单条调用 | 调试问题时贴 issue |

---

## 13. 总结

**实现路径选择**：选择"文件存储 + JSONL 索引"而非 SQLite，是因为：
- 不依赖数据库 schema，调试时可直接 `cat`/`jq`
- 写入是纯 append，并发安全简单
- 按日期分目录天然支持按日清空
- 总量可控（每日调用次数有界）

**ContextVar 关联 router 与 adapter**：避免改 adapter 函数签名，让"在现有 chat 调用栈上挂日志"零侵入。

**按会话分组 + 双视图**：解决"想看一次对话的全部调用"这一最常见诉求，比"按时间筛"更直观。

**原始 HTTP body 三层捕获**：业务层用于排查 LLM 决策，协议层用于排查 provider 错误，二者通过 call_id 关联。

整体而言，这套追溯系统把"对话可见性"从黑盒升级到白盒，调试 LLM 调用问题时不再需要去翻后端 stdout 日志。

---

## 14. 已知 Bug & 修复记录

### Bug #1 — `request.raw_http_body` 只存摘要，丢了 messages 和 tools

**现象**：详情 JSON 中 `request.raw_http_body` 只记录 `{model, max_tokens, messages_count, tools_count}`，没有真实的 `messages` 和 `tools` 数组，事后无法看到实际发给 provider 的完整 payload。

**根因**：adapter 层调用 `enrich_raw_body(cid, raw_request={model, max_tokens, ...})` 时，传的是手工构造的摘要 dict。

**修复**：直接传完整的 `payload` dict（含 messages / tools 数组），让原始 HTTP body 100% 落盘。

```python
# Before:
enrich_raw_body(cid, raw_request={
    "model": payload["model"],
    "max_tokens": payload["max_tokens"],
    "messages_count": len(messages),     # ← 只记了数量, 丢了实际消息
    "tools_count": len(tools or []),     # ← 同上
})

# After:
enrich_raw_body(call_id, raw_request=payload)  # ← 完整 payload
```

修复后详情文件中可看到：
```json
"raw_http_body": {
  "model": "qwen3:4b",
  "messages": [{"role": "system", "content": "你是 JARVIS..."}, ...],
  "max_tokens": 4096,
  "stream": true,
  "tools": [{"name": "file", "description": "...", ...}, ...]
}
```

---

### Bug #2 — 流式 chat_stream_full 不写入日志

**现象**：前端调用 `/api/chat/stream` 走的是 `router.chat_stream_full`，但日志目录里只看到 `router.chat`（来自 topic 生成），流式主对话记录缺失。

**根因**（双重）：
1. **ContextVar Token 跨 Context 失效**：router 用 `set_current_call_id(call_id)` 拿到 Token，在 `finally` 里 `reset_current_call_id(token)`。但 chat_stream_full 是 `async generator` —— 调用方 `chat_engine` 在 `async for` 中收到 `message_stop` 后 `break`，Python 会在 yield 处抛 `GeneratorExit` 关闭生成器。此时生成器在**不同的 Context** 中，调用 `reset_current_call_id(token)` 直接抛 `ValueError: <Token ...> was created in a different Context`，阻断后续的 `cl.end_call()`。
2. **fire-and-forget 的 task 被 GC**：即使没有这个 ValueError，原来的 `asyncio.create_task(self._flush(record))` 创建的 task 在生成器 aclose 时如果没有外部引用持有，会被立刻 GC，永远不被调度。

**修复**：
1. **彻底移除 ContextVar**：router 直接把 `call_id` 作为参数传给 adapter：

```python
# router.chat_stream_full
async def chat_stream_full(self, ..., *, conversation_id=None, **kwargs):
    rec = cl.start_call(...)
    async for event in client.chat_stream_full(messages, call_id=rec.call_id):
        ...
```

```python
# AnthropicAdapter.chat_stream_full
async def chat_stream_full(self, messages, *, call_id=None):
    enrich_raw_body(call_id, raw_request=payload)   # 显式传入
    async with self.client.stream(...) as resp:
        async for line in resp.aiter_lines():
            if call_id is not None:
                raw_sse_events.append(data)            # 不再用 contextvar
            ...
```

2. **`end_call` 同步写入**：在 `finally` 块里直接调用 `self._write_sync(record)`，不依赖 task 调度：

```python
finally:
    try:
        cl.end_call(rec, response={...}, status=..., error=...)
    except Exception as e:
        logger.error(...)
```

`finally` 在 async generator 的 aclose 流程中**总是会执行**，所以无论调用方 break / 流正常结束 / 异常，都保证落盘。

**验证结果**（4 条日志全部捕获）：

| call_id | source | conv | msgs | chars |
|---------|--------|------|------|-------|
| cl-4a7e78200a | router.chat_stream_full | v3-001 | 2 | 34 |
| cl-257a8c74e9 | router.chat | (topic) | 2 | 2 |
| cl-575ab09c97 | router.chat_stream_full | v3-002 | 3 | 14 |
| cl-6856f9d2fd | router.chat | v3-002 | 4 | 369 |

---

### Bug #3 — 一次对话触发多次 LLM 调用（设计澄清）

**澄清**：用户指出"用户的一次输出可能会有多个请求"，这是 jarvis 的核心架构特征：

```
一次 chat 输出 = router.chat_stream_full (Phase 1)
                + N 次 router.chat (工具循环 iter 2..N)
                + topic_generator → router.chat (生成对话标题，可并发或顺序)
```

每条调用都是**独立的 call_id**，但通过 `conversation_id` 关联。日志系统按此设计：
- `index.jsonl` 只存摘要（call_id, conversation_id, msgs count, latency, status）
- 详情文件存完整记录
- 前端"按会话"视图聚合同 conv_id 的所有 call

**为什么用参数而不是 ContextVar**：
- 同一 chat 内的多层调用（chat_engine → agent_loop → router.chat → adapter）会嵌套多层 Context，Token 在不同层会失效
- 用显式参数传递 call_id 完全规避了 ContextVar 的复杂性
- 代价：必须改 adapter 签名（chat / chat_stream / chat_stream_full 多了 `call_id` 参数，默认 None）

---

## 15. 文件清单（最终）

### 新增（4 个）
| 文件 | 作用 |
|------|------|
| `jarvis/services/ai/call_logger.py` | LLMCallLogger + LLMCallRecord + enrich_raw_body |
| `jarvis/api/logs.py` | 5 个 API 端点 |
| `frontend/src/stores/llmCallLogs.ts` | Pinia store + groupedByConversation computed |
| `frontend/src/components/LLMCallLogViewer.vue` | 双视图 viewer (时间/会话) |

### 修改（5 个）
| 文件 | 关键改动 |
|------|---------|
| `jarvis/services/ai/router.py` | chat/chat_stream_full 接 call_id，finally 同步写日志 |
| `jarvis/services/ai/base.py` | AIClient.chat / chat_stream / chat_stream_full 加 call_id kwarg |
| `jarvis/services/ai/providers/anthropic.py` | 显式接 call_id，写完整 payload + SSE chunks |
| `jarvis/services/ai/providers/openai.py` | 同上 |
| `jarvis/core/chat_engine.py` | router 调用传 conversation_id（已在前一轮加） |
| `frontend/src/components/Settings.vue` | 侧栏加 LLM 日志 section |

### 测试

| 验证项 | 结果 |
|--------|------|
| 后端模块导入 | ✅ |
| 4 个调用全部落盘 | ✅ |
| raw_http_body 含完整 messages + tools | ✅ |
| 流式 chat_stream_full 正常记录 | ✅ |
| 流中断 / 异常路径落盘 | ✅ (finally 块保证) |
| `tests/test_chat_engine.py` 35/35 | ✅ regression 通过 |
| vue-tsc 类型检查 | ✅ 0 error |
