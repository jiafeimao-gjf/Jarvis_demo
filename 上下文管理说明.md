# 上下文管理模块说明 (ContextManager + ContextCompressor)

> 最后更新：2026-07-02
> 适用版本：JARVIS v2（含 ContextManager + ContextCompressor + SubagentOrchestrator）

---

## 🎯 一句话总结

**ContextManager 在每次 LLM 调用之前，把「system prompt + 相关记忆 + 对话历史」按 token 预算打包，把超出预算的早期对话压缩或丢弃，让送入 LLM 的消息列表既完整又不爆 context。**
**ContextCompressor 在每轮对话后，监控上下文用量，超阈值时自动调 LLM 摘要早期消息并替换，让长对话也能保持健康。**

---

## 🎯 一句话总结

**ContextManager 在每次 LLM 调用之前，把「system prompt + 相关记忆 + 对话历史」按 token 预算打包，把超出预算的早期对话压缩或丢弃，让送入 LLM 的消息列表既完整又不爆 context。**

---

## 📍 为什么需要它

### 改之前的痛点

`ChatEngine` 在三个调用入口 (`chat()` / `stream_chat()` / `stream_chat_with_messages()`) 都硬编码：

```python
for msg in self.current_conversation.get_history(limit=10):
    messages.append({"role": msg.role, "content": msg.content})
```

带来的问题：

1. **早期消息永久丢失** — 超过 10 条就看不到上下文，LLM 失忆
2. **没有 token 预算** — system_prompt + 10 条长消息直接撑爆小模型 context
3. **记忆注入 ad-hoc** — `chat()` 里写一份 `memory.retrieve()`，`stream_chat_*` 不注入，不一致
4. **tool/tool_result 消息无清理** — 长对话下越聊越长，撑爆 context

### 改之后的收益

- ✅ 长对话不丢上下文（早期消息 LLM 摘要压缩）
- ✅ Token 预算自动按模型 context_window 算
- ✅ 记忆注入统一在 `build_messages()` 内，三个入口行为一致
- ✅ 压缩策略可插拔（Strategy Pattern），将来要换更聪明的压缩算法只需替换一个类

---

## 🔄 完整调用链

```
用户发送消息 "帮我看下昨天的代码"
         │
         ▼
ChatEngine.chat()
  ├─ 加载对话 → self.current_conversation (50 条历史)
  ├─ 构造 base_system_prompt (persona + skills + ...)
  │
  ├─ 调用 ──▶ ContextManager.build_messages(
  │             system_prompt=...,
  │             history=[最近 10 条消息],
  │             current_user_input="帮我看下昨天的代码",
  │             memory_retriever=memory.retrieve,
  │             model_id="qwen3:4b",
  │           )
  │             │
  │             ├─ 1) 查 MODELS["qwen3:4b"].context_window (默认 8192)
  │             ├─ 2) 构造 ContextBudget
  │             │     ├─ max=8192, output=1024, system=实测, memory=800
  │             │     └─ available_for_history ≈ 6000
  │             │
  │             ├─ 3) memory.retrieve("帮我看下昨天的代码", top_k=3)
  │             │     └─ 把命中的 3 条记忆拼到 system_prompt 末尾
  │             │
  │             ├─ 4) HybridStrategy.compact(history, budget)
  │             │     ├─ messages_tokens(history) > 6000 ?
  │             │     │   ├─ 否 → 原文全保留
  │             │     │   └─ 是 → 切两段:
  │             │     │        ├─ 前段: 调用 LLM 摘要（本项目未传 summarizer，降级为滑动窗口）
  │             │     │        └─ 后段: 原文保留最近 min_keep_recent=4 条
  │             │
  │             └─ 5) 拼成 [{system}, ...history, user]
  │                  返回 {messages, stats}
  │
  ├─ router.chat(messages, model="qwen3:4b")
  │     └─ 实际送入 Ollama 的就是这个 messages
  │
  └─ 保存对话 → DB
```

---

## 📦 实际送入 LLM 的 messages 长什么样

以 **qwen3:4b + 50 条历史** 为例，假设实际触发压缩：

```python
[
    {
        "role": "system",
        "content": (
            "# JARVIS 角色\n你叫 Jarvis...\n"
            "## 可用技能\n- pdf: 处理 PDF\n- code: 编码工作流\n"
            "\n相关记忆:\n- 用户偏好 Python\n- 昨天讨论过快排实现"   # ← 自动注入
        )
    },
    # === 摘要段 (如果有 summarizer) ===
    {
        "role": "system",
        "content": "[历史对话摘要 — ...]\n早期 46 条消息的主题: 用户问过 X/Y/Z, 解决了 ..."
    },
    # === 原文段 (按 token 倒推保留) ===
    {"role": "user",      "content": "...msg47"},
    {"role": "assistant", "content": "...msg48"},
    {"role": "user",      "content": "...msg49"},
    {"role": "assistant", "content": "...msg50"},
    # === 当前输入 ===
    {"role": "user", "content": "帮我看下昨天的代码"},
]
```

返回值里的 `stats` 字段（写入日志方便调优）：

```python
{
    "history_in": 50,            # 传入 50 条
    "history_out": 4,            # 实际只送 4 条原文
    "dropped": 46,               # 丢/压了 46 条
    "summary_added": True,       # 注入了早期摘要
    "memory_chunks": 3,          # 注入了 3 条相关记忆
    "tokens_estimate": 1234,     # 总 token 估算
    "budget_available": 6000,    # 历史可用 token 预算
}
```

---

## 🧠 三大策略的实际行为

### 场景 A：对话很短（10 条以内，总 < 预算）

`SlidingWindowStrategy` 和 `SummarizationStrategy` 都直接返回原文，不做任何压缩。

```
输入:  [u1, a1, u2, a2, ... u10, a10]  (200 tokens)
输出:  原样返回
stats: dropped=0, summary_added=False
```

### 场景 B：对话很长，超预算

`SummarizationStrategy` 会尝试切两段：

```
输入:  [u1, a1, u2, a2, ... u50, a50]  (12000 tokens, 预算 6000)

判定:
  - 保留: 尾部 min_keep_recent=4 条原文 (~400 tokens)
  - 丢给 summarizer: 前 46 条 (~11600 tokens)
  - summarizer 调用 LLM 压缩 → "用户在 5/20-5/25 期间讨论了快排/二分查找..."
  - 拼成: [summary_msg(200 tokens), ...4 条原文]
```

**关键**：当 `summarizer=None`（当前 ChatEngine 没有传），自动降级为滑动窗口，只保留尾部 `min_keep_recent` 条原文，不调 LLM 摘要，避免每次对话都额外花一次 LLM 调用。

### 场景 C：HybridStrategy（默认）

= `SummarizationStrategy` 的实现逻辑 + 三种组合：记忆注入 + 摘要压缩 + 滑动窗口兜底。

---

## ⚙️ 关键配置参数（ContextBudget）

| 参数 | 默认值 | 作用 |
|---|---|---|
| `max_context_window` | 从 MODELS 取，兜底 8192 | 总预算 |
| `reserve_for_output` | 1024 | 留给 LLM 输出 |
| `reserve_for_memory` | 800 | 相关记忆最多占 800 token |
| `reserve_for_system` | **动态测量** | system_prompt 实际用了多少算多少 |
| `min_keep_recent` | 4 | 无论如何保留最近 4 条原文 |
| `available_for_history` | `max - output - system - memory` | 历史可用，**最低 512** |

`available_for_history` 永远不会低于 512，避免预算算成负数。

---

## 🔌 如何自定义策略

```python
from jarvis.core.context_manager import (
    ContextManager, CompactionStrategy, CompactionResult
)

class MyStrategy(CompactionStrategy):
    async def compact(self, history, budget, summarizer=None):
        # 自定义压缩逻辑，例如：
        # - 按时间窗口分组
        # - 按 token 类型（代码 vs 自然语言）区分处理
        # - 永远保留 system 和最近 N 条
        return CompactionResult(messages=..., dropped_count=...)

# 注入到 ChatEngine
chat_engine.context_manager = ContextManager(strategy=MyStrategy())
```

---

## 🧩 模块结构

```
jarvis/core/context_manager.py
├── count_tokens(text)              # tiktoken 优先，回退 4 字符/token
├── messages_tokens(messages)       # 整组消息 token 估算（含 4 token 角色开销）
├── ContextBudget                   # token 预算数据类
│   └── available_for_history       # 计算属性
├── CompactionResult                # 压缩结果数据类
├── CompactionStrategy (ABC)        # 策略接口
│   ├── SlidingWindowStrategy       # 仅按 token 保留最近
│   ├── SummarizationStrategy       # 早期 LLM 摘要 + 尾部原文
│   └── HybridStrategy (default)    # 默认（= Summarization）
└── ContextManager                  # 入口
    ├── context_window_for(model)   # 查 MODELS 注册表
    ├── budget_for(model, ...)      # 构造预算
    └── build_messages(...)         # 主入口，返回 {messages, stats}

jarvis/core/context_compressor.py    ← v2.1 新增
├── ContextUsage                    # 用量快照（total / budget / ratio）
├── CompressedContext               # 压缩结果（含 summary + 统计）
├── CompressionTrigger (ABC)        # 触发策略接口
│   ├── ThresholdTrigger            # 简单阈值（默认 0.75）
│   └── AdaptiveTrigger             # 阈值 + 增长斜率
├── default_summarize(router, msgs) # 默认 LLM 摘要器
└── ContextCompressor               # 主入口
    ├── measure(conv, model_id)     # 测量用量
    ├── should_compress(conv)       # 纯判断（无副作用）
    ├── maybe_compress(conv)        # 检查 + 可能压缩（idempotent）
    └── get_stats(conv_id)          # 压缩统计
```

---

## 💡 实际生效位置

| 调用点 | 文件位置 | 改动 |
|---|---|---|
| `ChatEngine.chat()` | `chat_engine.py` L215 附近 | 替换 `get_history(limit=10)` |
| `ChatEngine.stream_chat()` | `chat_engine.py` L430 附近 | 同上 |
| `ChatEngine.stream_chat_with_messages()` | `chat_engine.py` L700 附近 | 同上 |
| 记忆注入 | 内置在 `build_messages()` | 不再需要 `ChatEngine` 自己 `memory.retrieve()` |
| 主题生成 | `topic_generator.py` | 保持独立，未走 ContextManager |

---

## 🧪 怎么验证生效

启动后端观察日志，每次对话会打印：

```
[Chat] ContextManager | history_in=50 history_out=4 dropped=46
       memory=3 tokens~=1234 budget=6000
```

如果看到 `dropped > 0`，说明压缩真的在跑；如果 `memory > 0`，说明相关记忆真的注入了。

可以在 `tests/test_context_manager.py` 里看 `TestBuildMessages::test_stats_reflect_compaction` 这个测试用例——它构造了 50 条 5000 字符的消息，断言 `dropped > 0`、`history_out < history_in`，是端到端验证压缩生效的标准用例。

---

## 📊 测试覆盖

`tests/test_context_manager.py` 包含 28 个用例，覆盖：

- `TestTokenCounting` — token 计数（中文/英文/Anthropic content_blocks）
- `TestSlidingWindow` — 三种场景（取尾 N / 超预算裁剪 / 短历史不丢）
- `TestSummarization` — 不超预算、缺 summarizer 降级、调 summarizer、summarizer 异常降级
- `TestBuildMessages` — 基础构建、记忆注入、retriever 失败容错、token 预算（已知/未知模型）、压缩生效、image 透传
- `TestHybridStrategy` — 委派给 Summarization
- `TestContextBudget` — 预算计算、512 下限

总计 **135 个测试全部通过**（28 ContextManager + 24 Subagent + 83 原有）。

---

## 🚧 已知局限 / 后续可优化

1. ✅ **真实 LLM 摘要已实现** — v2.1 起由 `ContextCompressor` 通过 AIRouter 调用 LLM 摘要早期消息，自动持久化。原来的 `SummarizationStrategy` 在 ChatEngine 主流程未传 summarizer 的问题已绕开。

2. **`simple_embed` 是 hash 伪 embedding** — `MemoryStore.simple_embed()` 用 hash 算"向量"，不是真实语义。要替换只需在 `MemoryStore.retrieve` 里换 embedding 模型。

3. **记忆注入没有去重** — top_k=3 拿到的 3 条记忆可能和当前对话高度相关，造成冗余。可在 `ContextManager.build_messages` 里加一步"和最近 3 条对话的去重"。

4. **压缩时机偏被动** — 现在每次构建都算 token。可以加缓存：`history_token_hash` 不变就不重算。

5. **压缩状态没持久化** — `ContextCompressor._last_compress_at / _compress_count` 在进程内存里，重启就丢。要长期跟踪需要外存到 DB。

---

## 📚 相关文档

- `CLAUDE.md` — Context Management 章节
- `DESIGN_PATTERNS.md` — Strategy Pattern 用法
- `tests/test_context_manager.py` — ContextManager 单元测试
- `tests/test_context_compressor.py` — ContextCompressor 单元测试
- `jarvis_architecture_v2.puml` — 架构图（ContextManager 橙色高亮）

---

# ContextCompressor — 上下文自动压缩 (v2.1 新增)

## 🎯 解决什么问题

`ContextManager` 的 `SummarizationStrategy` 在压缩时需要一个 `summarizer` 可调用对象 — 之前 `ChatEngine` 没传，所以**实际生产中总是降级为滑动窗口，没有真正的 LLM 摘要**。

`ContextCompressor` 解决了三个问题：

1. **真 LLM 摘要** — 通过 AIRouter 调一次 LLM 把早期消息压缩成结构化摘要
2. **per-conversation 状态** — 跟踪每个对话的压缩次数、最后压缩时间
3. **智能触发** — 不每次都跑（浪费 token），等用量超阈值才压缩
4. **冷却机制** — 同一对话 30s 内不重复触发，避免抖动

## 🔄 与 ContextManager 的分工

```
┌──────────────────────────────────────────────────────────────┐
│ 用户发送消息                                                  │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ ChatEngine.chat() / stream_chat()                            │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
┌──────────────────────────────────────────────────────────────┐
│ ContextManager.build_messages(..., conversation=conv)        │
│   ├─ 0) [NEW] if compressor:                                 │
│   │      result = await compressor.maybe_compress(conv)      │
│   │      if result: history = 从 conv.messages 重新读         │
│   │                                                        │
│   ├─ 1) 检索相关记忆 → 注入到 system_prompt 末尾              │
│   ├─ 2) HybridStrategy.compact(history, budget)              │
│   └─ 3) 拼成 [{system}, ...history, user]                    │
└────────────────────────┬─────────────────────────────────────┘
                         ▼
                  LLM 调用
```

| 组件 | 时机 | 粒度 | 作用 |
|---|---|---|---|
| **ContextManager** | 每次 LLM 调用前 | per-call | 决定本次送哪些消息（短期、静态策略） |
| **ContextCompressor** | 每次 chat 后 | per-conversation | 决定是否把历史压缩为摘要（长期、状态机） |

## 🚦 自动触发的判断逻辑

`should_compress(conversation, model_id)` 返回 True 当且仅当所有条件满足：

```python
1. len(conversation.messages) >= min_messages  (默认 6)
2. usage = measure(conv, model_id)
   usage.ratio = total_tokens / budget_tokens
3. trigger.should_compress(usage)  # 阈值判断
4. cooldown elapsed  (默认 30s, 同一对话内)
```

## 📊 默认配置

```python
ContextCompressor(
    router=chat_engine.router,
    trigger=ThresholdTrigger(threshold=0.75),  # 75% 触发
    keep_recent=4,           # 保留最近 4 条原文
    cooldown_seconds=30.0,   # 30s 冷却
    min_messages=6,          # 至少 6 条才值得压缩
    persist_fn=save_fn,      # 压缩后写回 DB
    summarizer=default_summarize,  # 默认 LLM 摘要器
)
```

**日志样例**：

```
[Compressor] init | trigger=ThresholdTrigger(threshold=0.75) keep_recent=4 cooldown=30.0s min_messages=6
[Compressor] 触发 | conv=a1b2c3d4... msgs=50 summarize=46 keep=4 before_tokens=23456
[Compressor] 完成 | dropped=46 kept=4 23456→812 tokens ratio=28.89x elapsed=3215ms
[Context] per-conversation 压缩完成 | dropped=46 ratio=28.89x
```

## 🔌 触发策略 (Trigger)

### ThresholdTrigger（默认）

```python
ThresholdTrigger(threshold=0.75)  # 用量 ≥ 75% 触发
```

简单直观，适合大多数场景。

### AdaptiveTrigger（高级）

```python
AdaptiveTrigger(
    threshold=0.75,             # 总量阈值
    growth_threshold_tokens=1500,  # 增长阈值 (距上次压缩)
)
```

满足任一即触发：
- 用量 ≥ 75% context_window
- 距上次压缩 token 增长 ≥ 1500

适合"对话可能在中等长度就反复讨论"的场景。

## 💻 实际压缩流程

```
输入: 50 条历史消息 (23456 tokens, 90% of 8192 budget)

1. measure() → ContextUsage(total=23456, budget=8192, ratio=0.91, ...)
2. should_compress():
   ✓ len(messages)=50 >= 6
   ✓ trigger.should_compress() → ratio >= 0.75
   ✓ cooldown 已过 (上次 5 分钟前)
3. _do_compress():
   ├─ msgs_to_summarize = msgs[:-4]  → 46 条
   ├─ kept = msgs[-4:]                → 4 条原文
   ├─ summary = await default_summarize(router, msgs_to_summarize)
   │   (一次 LLM 调用, 生成约 800 token 摘要)
   ├─ conversation.messages = [summary_msg, *kept]
   │   (从 50 条变成 5 条: 1 摘要 + 4 原文)
   ├─ after_tokens ≈ 1200 (从 23456 降到 1200)
   └─ persist_fn(conversation)  → 写回 DB
4. 返回 CompressedContext(to_dict 可观测)
```

## 🔍 监控 / 调试

```python
# 查看压缩统计
compressor.get_stats("conv-id-xxx")
# → {'conversation_id': 'conv-id-xxx', 'compress_count': 3, 'last_compress_at': 1720...}

# 重置 (例如删除对话后)
compressor.reset_stats("conv-id-xxx")
compressor.reset_stats()  # 重置全部

# 强制压缩 (跳过所有判断)
result = await compressor.maybe_compress(conv, force=True)
```

## ⚙️ 自定义触发

```python
class MyTrigger(CompressionTrigger):
    def should_compress(self, usage):
        # 例如: 模型是 qwen 时 0.8 触发, gpt 时 0.95 才触发
        ...

    def describe(self):
        return "MyTrigger(...)"

compressor = ContextCompressor(router, trigger=MyTrigger(...))
```

## 🚧 已知局限

1. **持久化依赖注入** — 必须传 `persist_fn`，否则只改内存不改 DB。当前 ChatEngine 注入的是 `_save_conversation_to_file`（写 JSON 文件），不是 SQLite。如果重启进程会丢压缩状态。建议同时在 `memory_store.save_conversation` 也写一次。

2. **摘要质量依赖模型** — 小模型（qwen3:4b）摘要质量有限，可能丢失关键细节。可在 `summarizer` 参数里传一个更聪明的摘要函数。

3. **没有去重** — 多次压缩后历史里可能出现多条 summary_msg。可在 `_do_compress` 里加：检测到已有 summary_msg 时把新摘要合进去。

4. **压缩时机偏被动** — 等用量到 75% 才压缩。如果对话一开始就快增长（用户连续抛问题），第一次压缩前就已经很挤了。可改 `AdaptiveTrigger` 用更低的 growth_threshold。

## 📚 测试覆盖

`tests/test_context_compressor.py` 36 个用例：
- `TestThresholdTrigger` — 阈值边界 / 非法值 / describe
- `TestAdaptiveTrigger` — 双路径触发 / describe
- `TestContextUsage` — ratio / over_threshold / to_dict
- `TestDefaultSummarize` — Router 调用 / Anthropic blocks / 异常降级
- `TestMeasure` — 空 / 填充 / 模型 context_window
- `TestShouldCompress` — 消息数 / 阈值 / 上方
- `TestMaybeCompress` — 不触发 / force / 消息替换 / 空摘要 / 持久化调用 / 持久化失败降级
- `TestCooldown` — 冷却阻止重复 / 冷却过期后再次触发
- `TestStats` — 计数 / reset 单个 / reset 全部
- `TestIntegrationWithContextManager` — 触发压缩 / 无 compressor 不压缩 / compressor 异常降级
- `TestCompressedContext` — to_dict 字段

总计 **171 个测试全部通过**（36 ContextCompressor + 28 ContextManager + 24 Subagent + 83 原有）。

---

*文档随模块迭代更新，版本 v2.0*